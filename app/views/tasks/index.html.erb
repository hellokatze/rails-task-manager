<h1>All my tasks</h1>
<p><% @tasks.each do |task| %>
<ul><%= link_to task.title, "/tasks/#{task.id}" %>
<%= link_to "Delete", tasks_path(task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></ul>
<% end %>
</p>

<%= link_to "Add new task", "/tasks/new" %>
